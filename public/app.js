const canvas=document.getElementById('game'),ctx=canvas.getContext('2d');
const ui={caseNo:document.getElementById('caseNo'),score:document.getElementById('score'),status:document.getElementById('status'),mission:document.getElementById('missionLabel'),hud:document.getElementById('hudText'),start:document.getElementById('startBtn'),pause:document.getElementById('pauseBtn'),secure:document.getElementById('secureBtn'),reset:document.getElementById('resetBtn'),district:document.getElementById('district')};
const S={x:220,y:570,target:{x:970,y:180},score:0,caseNo:1,running:false,paused:false,t:0,keys:new Set()};
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
function newTarget(){S.target={x:170+Math.random()*940,y:125+Math.random()*470};}
function road(x,y,w,h){ctx.fillStyle='#18262b';ctx.fillRect(x,y,w,h);ctx.strokeStyle='#304249';ctx.lineWidth=3;ctx.strokeRect(x,y,w,h);ctx.setLineDash([24,18]);ctx.strokeStyle='#52636a';ctx.beginPath();ctx.moveTo(x+w/2,y);ctx.lineTo(x+w/2,y+h);ctx.stroke();ctx.setLineDash([])}
function draw(){const W=canvas.width,H=canvas.height;ctx.clearRect(0,0,W,H);ctx.fillStyle='#0a1719';ctx.fillRect(0,0,W,H);
for(let y=0;y<H;y+=92)for(let x=0;x<W;x+=116){ctx.fillStyle=(x/116+y/92)%2?'#102024':'#0d1c20';ctx.fillRect(x+3,y+3,110,86);}
road(0,350,W,150);road(470,0,145,H);road(800,0,120,H);road(120,0,100,H);
for(let i=0;i<28;i++){const x=(i*173)%W,y=(i*97)%H,s=38+(i%4)*13;ctx.fillStyle=i%3?'#1c3035':'#21383d';ctx.fillRect(x,y,s,s*1.35);ctx.fillStyle='#335158';ctx.fillRect(x+8,y+10,s-16,3)}
ctx.fillStyle='#6be89a';ctx.shadowColor='#6be89a';ctx.shadowBlur=22;ctx.beginPath();ctx.arc(S.target.x,S.target.y,19+Math.sin(S.t/8)*3,0,Math.PI*2);ctx.fill();ctx.shadowBlur=0;ctx.fillStyle='#07120d';ctx.beginPath();ctx.arc(S.target.x,S.target.y,6,0,Math.PI*2);ctx.fill();ctx.fillStyle='#8ff0ae';ctx.font='800 15px Inter,system-ui';ctx.fillText('INCIDENT',S.target.x-38,S.target.y-34);
const a=Math.atan2(S.target.y-S.y,S.target.x-S.x);ctx.save();ctx.translate(S.x,S.y);ctx.rotate(a);ctx.fillStyle='#65e89a';ctx.fillRect(-28,-18,56,36);ctx.fillStyle='#b7c8cb';ctx.fillRect(-14,-25,28,12);ctx.fillStyle='#081113';ctx.fillRect(8,-10,13,20);ctx.fillStyle='#dfffe9';ctx.fillRect(-31,-14,7,10);ctx.fillRect(-31,4,7,10);ctx.restore();
if(S.running&&!S.paused){let dx=0,dy=0;if(S.keys.has('a')||S.keys.has('arrowleft'))dx--;if(S.keys.has('d')||S.keys.has('arrowright'))dx++;if(S.keys.has('w')||S.keys.has('arrowup'))dy--;if(S.keys.has('s')||S.keys.has('arrowdown'))dy++;const n=Math.hypot(dx,dy)||1;S.x=clamp(S.x+dx/n*4.6,28,W-28);S.y=clamp(S.y+dy/n*4.6,55,H-35);S.t++;const d=Math.hypot(S.x-S.target.x,S.y-S.target.y);ui.status.textContent=d<65?'Incident reached — secure the scene.':'Navigate to the green incident marker.';ui.hud.textContent=d<65?'INCIDENT IN RANGE':'PATROL ACTIVE'}requestAnimationFrame(draw)}
function start(){S.running=true;S.paused=false;ui.status.textContent='Patrol started. Locate the green incident marker.';ui.hud.textContent='PATROL ACTIVE'}
function pause(){if(!S.running)return;S.paused=!S.paused;ui.pause.textContent=S.paused?'Resume':'Pause';ui.status.textContent=S.paused?'Patrol paused.':'Patrol resumed.';ui.hud.textContent=S.paused?'PATROL PAUSED':'PATROL ACTIVE'}
function secure(){if(!S.running||S.paused)return;const d=Math.hypot(S.x-S.target.x,S.y-S.target.y);if(d<70){S.score+=100;S.caseNo++;newTarget();ui.score.textContent=String(S.score).padStart(6,'0');ui.caseNo.textContent=String(S.caseNo).padStart(2,'0');ui.status.textContent='Scene secured. New incident dispatched.';ui.hud.textContent='NEW INCIDENT'}else ui.status.textContent='Too far away. Get closer to the green marker.'}
function reset(){S.x=220;S.y=570;S.score=0;S.caseNo=1;S.running=false;S.paused=false;S.t=0;newTarget();ui.score.textContent='000000';ui.caseNo.textContent='01';ui.pause.textContent='Pause';ui.status.textContent='Start your patrol.';ui.hud.textContent='PATROL STANDBY'}
ui.start.onclick=start;ui.pause.onclick=pause;ui.secure.onclick=secure;ui.reset.onclick=reset;
document.addEventListener('keydown',e=>{const k={KeyW:'w',KeyA:'a',KeyS:'s',KeyD:'d',ArrowUp:'arrowup',ArrowDown:'arrowdown',ArrowLeft:'arrowleft',ArrowRight:'arrowright'}[e.code];if(k){e.preventDefault();S.keys.add(k)}});document.addEventListener('keyup',e=>{const k={KeyW:'w',KeyA:'a',KeyS:'s',KeyD:'d',ArrowUp:'arrowup',ArrowDown:'arrowdown',ArrowLeft:'arrowleft',ArrowRight:'arrowright'}[e.code];if(k)S.keys.delete(k)});
document.querySelectorAll('[data-key]').forEach(b=>{const k=b.dataset.key;const down=e=>{e.preventDefault();S.keys.add(k)};const up=e=>{e.preventDefault();S.keys.delete(k)};b.addEventListener('pointerdown',down);b.addEventListener('pointerup',up);b.addEventListener('pointercancel',up);b.addEventListener('pointerleave',up)});canvas.addEventListener('pointerdown',()=>{if(!S.running)start()});newTarget();draw();